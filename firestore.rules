
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all read and write access to the entire database.
    // WARNING: This is insecure and not recommended for production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
