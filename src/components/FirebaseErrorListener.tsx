'use client';

// This component is no longer needed as we are not throwing custom permission errors.
// It can be removed in a future step, but for now, it's safer to just nullify its effect.

export function FirebaseErrorListener() {
  // This component now does nothing and renders nothing.
  return null;
}
